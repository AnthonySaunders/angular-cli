## Angular CLI + WP RestFUL API integration basic

A basic angular app that hooks into a local wordpress installation (http://localhost/wordpress) and pulls posts in list format
using the WP 4.7 Rest API